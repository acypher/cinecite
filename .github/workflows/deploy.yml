name: Build and Deploy

on:
  push:
    branches: [main]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      DEPLOY_USER: ${{ secrets.SFTP_USERNAME }}
      DEPLOY_HOST: ${{ secrets.SFTP_HOST }}
      # Absolute path on InMotion (site lives under /home/thekla5).
      REMOTE_DIST: /home/thekla5/cinecite.acypher.com/dist

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p 2222 "$DEPLOY_HOST" >> ~/.ssh/known_hosts

      - name: Deploy via rsync
        run: |
          set -euo pipefail
          SSH=(ssh -i "$HOME/.ssh/deploy_key" -p 2222 -o BatchMode=yes)
          REMOTE="${DEPLOY_USER}@${DEPLOY_HOST}"

          "${SSH[@]}" "$REMOTE" "mkdir -p '${REMOTE_DIST}/public'"

          RSYNC=(rsync -avz --human-readable -e "ssh -i $HOME/.ssh/deploy_key -p 2222 -o BatchMode=yes")

          # Server bundle
          "${RSYNC[@]}" ./dist/index.cjs "$REMOTE:${REMOTE_DIST}/"

          # Vite output: trailing slash on source copies *contents* into .../public/
          "${RSYNC[@]}" --delete ./dist/public/ "$REMOTE:${REMOTE_DIST}/public/"

      - name: Verify deployment
        run: |
          ssh -i ~/.ssh/deploy_key -p 2222 -o BatchMode=yes \
            "${DEPLOY_USER}@${DEPLOY_HOST}" \
            "ls -la '${REMOTE_DIST}/' && ls -la '${REMOTE_DIST}/public/'"
